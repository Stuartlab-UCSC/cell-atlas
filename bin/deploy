#!/bin/bash

# Build a production bundle and deploy it.
# Run from the dir: cell

npm run build
rsync -rtp build/ hexmap:dev/cell/build/

echo
echo If the rsync session timed out, do:
echo "rsync -rtp /build/ hexmap:dev/cell/build/"

