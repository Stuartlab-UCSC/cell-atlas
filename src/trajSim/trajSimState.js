
const reducers = {
    /*
    'trajSim.metadataShow': (state = false, action) => {
        if (action.type === 'moleSim.metadataShow.toggle') {
            return !state
        } else {
            return state
        }
    },
    */
}

export default reducers
