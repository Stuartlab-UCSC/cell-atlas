
// The name to label, tooltip translations.

const sizeRef = {
    similarity: {
        label: 'similarity',
        tooltip: 'weighted correlation of pathway activity scores',
    },
}

export { sizeRef }
